-- ✅ MẪU CHUẨN CHO NGƯỜI MỚI | TÉO HUB 🍎
-- Giữ nguyên giao diện thật, chỉ thêm hướng dẫn 👈 ngay bên cạnh dòng cần sửa

repeat task.wait() until game:IsLoaded()

-- 🟢 Nút mở menu chính
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.10615778, 0, 0.16217947, 0)
ImageButton.Size = UDim2.new(0, 40, 0, 40)
ImageButton.Draggable = true
ImageButton.Image = "http://www.roblox.com/asset/?id=76121098594454 "  -- 👈 đổi ID ảnh nút mở menu tại đây (ảnh roblox)

UICorner.CornerRadius = UDim.new(1, 10)
UICorner.Parent = ImageButton

ImageButton.MouseButton1Down:Connect(function()
	game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)

-- 🟣 Giao diện Fluent
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
repeat wait() until game:IsLoaded()

-- 🪟 Cửa sổ chính
local Window = Fluent:CreateWindow({
	Title = "💀Mất Quyền Truy Cập",         -- 👈 đổi thành tên script của bạn
	SubTitle = "Xqhub",              -- 👈 đổi mô tả phụ
	TabWidth = 157,
	Size = UDim2.fromOffset(450, 300),
	Acrylic = true,
	Theme = "Amethyst",                   -- 👈 đổi màu chủ đạo: Amethyst, Dark, Aqua, Light,...
	MinimizeKey = Enum.KeyCode.End
})

-- 📑 Các tab (mục chính)
local Tabs = {
	Main0 = Window:AddTab({ Title = "một số kênh🤓" }),  -- 👈 đổi tên tab 1
	Main1 = Window:AddTab({ Title = "FARM Tiền $" }),  -- 👈 đổi tên tab 2
	Main2 = Window:AddTab({ Title = "FARM BOSS 🤖" }),     -- 👈 đổi tên tab 3
	Main3 = Window:AddTab({ Title = "PVP⚔️" }),        -- 👈 đổi tên tab 4
	Main4 = Window:AddTab({ Title = "SHOP Bảo Trì" }),    -- 👈 đổi tên tab 5
	Main5 = Window:AddTab({ Title = "FARM NPC" }) -- 👈 đổi tên tab 6
}

------------------------------------------------
-- TAB 1: Các kênh tiktok
------------------------------------------------
Tabs.Main0:AddButton({
	Title = "@xun.quyn15",              -- 👈 tên nút
	Description = "Nhấn vào để sao chép link tiktok ",-- 👈 mô tả nút
	Callback = function()
		setclipboard("https://www.tiktok.com/@xun.quyn15?_r=1&_t=ZS-91pwZ8A7SQa ")  -- 👈 link sao chép
	end
})

Tabs.Main0:AddButton({
	Title = "tiktok",
	Description = "fl tiktok để biết thêm nhiều script",
	Callback = function()
		setclipboard("https://www.tiktok.com/@xun.quyn15?_r=1&_t=ZS-91pwZ8A7SQa ")
	end
})

------------------------------------------------
-- TAB 2: Ví dụ thêm script
------------------------------------------------
Tabs.Main1:AddButton({
    Title = "ANTI AFK",
    Description = "Không bị kick AFK 👍",
    Callback = function()

        -- Thông báo hiện ngay khi bật
        pcall(function()
            game.StarterGui:SetCore("SendNotification", {
                Title = "ANTI AFK";
                Text = "Anti AFK đã bật!";
                Duration = 4;
            })
        end)

        -- Code chống AFK
        local vu = game:GetService("VirtualUser")
        local player = game.Players.LocalPlayer

        task.spawn(function()
            while true do
                player.Idled:Wait()
                vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                task.wait(1)
                vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            end
        end)
    end
})

Tabs.Main2:AddButton({
	Title = "Farm boss",                             -- 👈 tên nút hiển thị
	Description = "nhấn vào để bật script ",-- 👈 mô tả
	Callback = function()
--========================================================--
--============  MENU THU NHỎ / MỞ RỘNG  ==================--
--========================================================--

local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")

-- GUI chính ở CoreGui (để tránh bị xoá bởi PlayerGui)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.Name = "FarmBossGUI"

--=============================
-- NÚT MỞ MENU
--=============================
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ScreenGui
ToggleButton.Size = UDim2.new(0, 55, 0, 55)
ToggleButton.Position = UDim2.new(0, 20, 0, 200)
ToggleButton.Image = "rbxassetid://3926307971"
ToggleButton.ImageRectOffset = Vector2.new(4, 4)
ToggleButton.ImageRectSize = Vector2.new(36, 36)
ToggleButton.BackgroundTransparency = 1

--=============================
-- FRAME MENU FARM BOSS
--=============================
local FarmBossMenu = Instance.new("Frame")
FarmBossMenu.Parent = ScreenGui
FarmBossMenu.Size = UDim2.new(0, 420, 0, 360)
FarmBossMenu.Position = UDim2.new(0.5, -210, 0.5, -180)
FarmBossMenu.BackgroundColor3 = Color3.fromRGB(40,40,40)
FarmBossMenu.Visible = false
FarmBossMenu.Active = true
FarmBossMenu.Draggable = true
FarmBossMenu.BorderSizePixel = 0

Instance.new("UICorner", FarmBossMenu).CornerRadius = UDim.new(0, 12)

--=============================
-- TITLE
--=============================
local Title = Instance.new("TextLabel")
Title.Parent = FarmBossMenu
Title.Size = UDim2.new(1, 0, 0, 45)
Title.BackgroundColor3 = Color3.fromRGB(20,20,20)
Title.Text = "FARM BOSS 👾"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 26
Title.TextColor3 = Color3.new(1,1,1)

--=============================
-- NÚT ĐÓNG MENU
--=============================
local CloseButton = Instance.new("TextButton")
CloseButton.Parent = FarmBossMenu
CloseButton.Size = UDim2.new(0, 40, 0, 40)
CloseButton.Position = UDim2.new(1, -50, 0, 3)
CloseButton.Text = "✖"
CloseButton.TextScaled = true
CloseButton.BackgroundTransparency = 1
CloseButton.TextColor3 = Color3.fromRGB(255,80,80)

CloseButton.MouseButton1Click:Connect(function()
    FarmBossMenu.Visible = false
end)

--=============================
-- NÚT BẬT AUTO FARM
--=============================
local BtnFarm = Instance.new("TextButton")
BtnFarm.Parent = FarmBossMenu
BtnFarm.Size = UDim2.new(1, -40, 0, 50)
BtnFarm.Position = UDim2.new(0, 20, 0, 70)
BtnFarm.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
BtnFarm.Text = "Bật Script Farm Boss"
BtnFarm.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnFarm.Font = Enum.Font.GothamBold
BtnFarm.TextSize = 22

Instance.new("UICorner", BtnFarm).CornerRadius = UDim.new(0, 10)

--=============================
-- LABEL HIỆN MÁU BOSS
--=============================
local hpLabel = Instance.new("TextLabel")
hpLabel.Parent = FarmBossMenu
hpLabel.Position = UDim2.new(0, 20, 0, 130)
hpLabel.Size = UDim2.new(1, -40, 0, 35)
hpLabel.Text = "Máu Boss: ..."
hpLabel.Font = Enum.Font.GothamBold
hpLabel.TextSize = 22
hpLabel.BackgroundTransparency = 1
hpLabel.TextColor3 = Color3.fromRGB(255, 60, 60)

--=============================
-- LOGIC TÌM BOSS
--=============================
local function findBoss()
    local nearest, dist = nil, math.huge
    for _, mob in ipairs(workspace:GetDescendants()) do
        if mob:FindFirstChild("Humanoid") and mob:FindFirstChild("HumanoidRootPart") then
            if mob.Humanoid.Health > 0 then
                local d = (mob.HumanoidRootPart.Position - hrp.Position).Magnitude
                if d < dist then
                    nearest = mob
                    dist = d
                end
            end
        end
    end
    return nearest
end

local function attack()
    local tool = char:FindFirstChildOfClass("Tool")
    if tool then tool:Activate() end
end

--=============================
-- BẬT/TẮT AUTO FARM
--=============================
local autoFarm = false

BtnFarm.MouseButton1Click:Connect(function()
    autoFarm = not autoFarm
    BtnFarm.Text = autoFarm and "TẮT Script Farm Boss" or "Bật Script Farm Boss"

    while autoFarm do
        task.wait(0.05)

        local boss = findBoss()
        if not boss then
            hpLabel.Text = "Không tìm thấy Boss!"
        else
            hpLabel.Text = "Máu Boss: " .. math.floor(boss.Humanoid.Health)
            
            -- đưa người chơi lên đầu boss để đánh
            local pos = boss.HumanoidRootPart.Position + Vector3.new(0, 25, 0)
            hrp.CFrame = CFrame.new(pos)

            attack()
        end
    end
end)

--=============================
-- NÚT THU NHỎ / MỞ RỘNG
--=============================
local Opened = false
ToggleButton.MouseButton1Click:Connect(function()
    Opened = not Opened
    FarmBossMenu.Visible = Opened
end)

print("Farm Boss GUI Loaded!")